const skippedProperties = ['isUpToUp', 'dartType', 'menu', 'skills', 'damInfo'];

const gamePublisher = 'TeaMobi';
const server = gamePublisher + '/Universe1';

document.addEventListener('DOMContentLoaded', function () {
    main();
});