const skippedProperties = ['isUpToUp', 'dartType', 'menu', 'skills', 'damInfo'];

const gamePublisher = 'HSNR';
const server = gamePublisher + '/Server1';

document.addEventListener('DOMContentLoaded', function () {
    main();
});